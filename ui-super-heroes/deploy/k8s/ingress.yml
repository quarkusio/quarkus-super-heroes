---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  labels:
    app: ui-super-heroes
    application: super-heroes
  name: ui-super-heroes
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: ui-super-heroes
                port:
                  name: http
            path: /
            pathType: Prefix
